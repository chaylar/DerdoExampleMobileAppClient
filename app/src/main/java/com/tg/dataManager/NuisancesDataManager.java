package com.tg.dataManager;

import java.util.ArrayList;
import com.tg.dataObject.NuisanceType;

public class NuisancesDataManager {

    /*public static ArrayList<NuisanceType> GetNuisances() {

        //TODO : GET THESE FROM API
        //String[] values = new String[] { "İşsizlik / Parasızlık / Borçlar", "İş Hayatı / Mobbing / Tükenmişlik", "Okul Hayatı / Sınav Stresi / Gelecek Kaygısı", "Sağlık Problemleri / Hastalıklar", "Aşk Acısı / Aldatılma / Ayrılık / Boşanma" };
        //String[] values = new String[] { "İşsizlik", "İş Hayatı", "Aşk Acısı", "Hastalık", "Sınav Stresi", "Diğer" };
        final ArrayList<NuisanceType> list = new ArrayList<NuisanceType>();
        for (int i = 0; i < values.length; ++i) {

            NuisanceType ndo = new NuisanceType();
            ndo.name = values[i];
            ndo.nuisanceCode = (i + 1);

            list.add(ndo);
        }

        return list;
    }*/

    public static void SelectNuisanceById(int id) {
        //TODO :
    }

}
